import type { Command } from "commander";
import { unimplemented } from "../utils.ts";

export default async function simulate(options: { deviceList: string, relays: string[] }, _command: Command) {
  unimplemented();
}
